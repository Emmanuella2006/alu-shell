#!/usr/bin/env bash
# This script lists the current directory content
# and displays only the part of each filename after the first dash

for file in *; do
  # Skip if not a regular file (optional safeguard)
  if [ -f "$file" ]; then
    # Cut the filename after the first '-'
    echo "${file#*-}"
  fi
done

